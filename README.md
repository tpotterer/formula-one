# Formula one bounding box notebook

Data required from aicrowd, download from cli:
- pip install -U aicrowd-cli
- aircrowd login --api-key API_KEY
- aicrowd dataset download --challenge f1-car-detection
